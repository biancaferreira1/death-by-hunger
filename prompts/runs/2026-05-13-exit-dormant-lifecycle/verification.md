# Verification: 2026-05-13-exit-dormant-lifecycle

```bash
git status --short
```

Manual: press **D**—figure should **move up immediately** (no long fade). After leaving top (**y < 0**), **~3 s** with no sprite, then figure **reappears** near bottom. Pool stays **300**; resize window during exit/dormant should stay stable.
