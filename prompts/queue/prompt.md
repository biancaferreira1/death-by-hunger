You must strictly refactor the life cycle of the existing 'people' objects in `@src/sketch.js` to eliminate the current "fade and disappear" behavior entirely, ensuring that no new or random objects are spawned to replace them. Instead, you are to re-route the exit logic so that the **existing** people move vertically upward off-screen the moment they are triggered to leave. Once a person’s Y-coordinate clears the top of the viewport ($y < 0$), you must implement a mandatory three-second timer—using a `setTimeout` or a frame-based counter—that keeps that specific person dormant before resetting their coordinates to the bottom of the window for a full respawn. It is critical that this loop applies only to the current people in the state and remains fully compatible with the responsive window-stretching layout, ensuring a continuous cycle of flying up and reappearing without any entities permanently vanishing.